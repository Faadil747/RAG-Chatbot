# Node
node_modules/
dist/
build/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Env files
.env
!.env.example

# ai-service local vector store / index (regenerated at runtime)
ai-service/data/qdrant/
ai-service/data/candidates_store.json

# backend uploaded resumes (local dev storage)
backend/uploads/*
!backend/uploads/.gitkeep

# local embedded Postgres (pgserver fallback when no Docker/Postgres install
# is available -- see README §14)
.pgdata/

# editor/OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.code-workspace
